{
  "name": "glyph-sheet-exporter",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uv run uvicorn server.app:app --host 127.0.0.1 --port 8000",
    "test": "node --test"
  }
}
